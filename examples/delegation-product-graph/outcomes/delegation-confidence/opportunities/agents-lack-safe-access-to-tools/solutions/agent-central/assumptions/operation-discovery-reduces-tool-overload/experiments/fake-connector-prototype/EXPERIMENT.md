# Fake Connector Prototype

```yaml
type: experiment
id: experiment.fake-connector-prototype
status: example
```

Build an in-memory connector prototype and observe whether agents can discover, describe, and execute the right operation without needing one MCP tool per capability.
