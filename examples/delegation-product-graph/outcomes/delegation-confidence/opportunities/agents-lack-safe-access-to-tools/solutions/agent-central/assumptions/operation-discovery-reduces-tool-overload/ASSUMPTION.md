# Operation Discovery Reduces Tool Overload

```yaml
type: assumption
id: assumption.operation-discovery-reduces-tool-overload
status: example
```

Agents will perform better when they can search and describe operations instead of receiving a very large static tool list.
