# Agent Central MVP

```yaml
type: prd
id: prd.agent-central-mvp
status: example
```

Validate the smallest useful loop for low-context agent operation discovery and execution.

## Acceptance Criteria

- Agents can search operations.
- Agents can describe a selected operation.
- Agents can execute a valid GraphQL operation.
- Runtime permission checks allow or deny execution.
